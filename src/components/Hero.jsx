import React from "react";
import Container from "../layout/Container";

const Hero = () => {
  return (
    <Container parentClass="hero">
      <button>Reserve Now</button>
    </Container>
  );
};

export default Hero;
